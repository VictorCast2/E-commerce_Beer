package com.E_commerce_Beer.repository;

import com.E_commerce_Beer.entity.Pack;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PackRepository extends JpaRepository<Pack, Long> {
}
